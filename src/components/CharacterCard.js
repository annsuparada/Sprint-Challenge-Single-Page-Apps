import React from 'react'

export default function CharacterCard (props) {
  const { name} = props
  return (
    <div>
      {/* <image className="" src={img} alt={name}/> */}
      <h2>{name}</h2>
      
      

    </div>
  )
}
